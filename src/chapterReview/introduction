容器collection
java.util包下的一些接口和类
与数组的两点不同：
1.数组的容量有限，collection没有限制，可以自动调节；
2.collection只能用来存放对象，而数组没有这样的限制

Collection两个子接口
 1.Set  无序不可重复
   HashSet
 2.List  有序可重复
   ArrayList 底层数组实现
   LinkedList 底层链表实现
   Vector 同ArrayList实现一样 底层数组  线程安全
 
 Map 键值对 key-value
  HaseMap